// arrow functions

const greet_user = (f_name) => console.log(`Helllo ${f_name}`);

greet_user("Jerry");

// another arrow fun

const sum2 = (a, b) => {
  return a + b;
};

console.log(sum2(23, 78));
