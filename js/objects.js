// objects in javascript

// basically objects we define key value pair

const employees = {
  fName: "Jake",
  salary: 340000,
  job: "DevOps",
  depts: ["Marketing", "Deployment", "Devlopment"],
};

console.log(employees);

// object operations

console.log(employees.depts);
console.log(employees.fName);
console.log(employees["job"]);

// objects methods
